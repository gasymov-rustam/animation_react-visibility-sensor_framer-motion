import { m } from 'framer-motion';

export const cls = {
  liStyle: 'mr-[3rem] hover:cursor-pointer transition duration-300 hover:opacity-25',
  ul: 'flex w-[100%] justify-between',
  wrapper: 'menu flex,',
};
